
import Vue from 'vue';
window.Vue = require('vue');

//Vue.component('notifyComponent', require('./components/notify.vue').default);

const app = new Vue({
    el: '#app',
});

'use strict'
let Alpine = require('alpinejs');
